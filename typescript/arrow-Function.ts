let anonim = function(message: string): string{
    return message;
};
// message est de type string et le retour de la fonction aussi

// avec arrow function ^^^

let affiche = (message: string): string =>{
    return message;
};


//ou sinon une autre ecriture de la fonction annonyme
let autre = () => {console.log('salam les amis');}

let aff =(message) => {
    return message;
};
